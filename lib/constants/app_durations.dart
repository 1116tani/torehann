// lib/constants/app_durations.dart

class AppDurations {
  // ─────────────────────────────────
  // ⚡ Fast
  // ─────────────────────────────────

  static const fast = Duration(
    milliseconds: 150,
  );

  static const normal = Duration(
    milliseconds: 280,
  );

  static const slow = Duration(
    milliseconds: 500,
  );

  // ─────────────────────────────────
  // ✨ UI Animation
  // ─────────────────────────────────

  static const button = Duration(
    milliseconds: 180,
  );

  static const card = Duration(
    milliseconds: 240,
  );

  static const modal = Duration(
    milliseconds: 320,
  );

  static const tabSwitch = Duration(
    milliseconds: 220,
  );

  static const pageTransition = Duration(
    milliseconds: 350,
  );

  // ─────────────────────────────────
  // 🗺 Navigation / Adventure
  // ─────────────────────────────────

  static const routeDraw = Duration(
    milliseconds: 1200,
  );

  static const fogAnimation = Duration(
    milliseconds: 1800,
  );

  static const compassRotation = Duration(
    milliseconds: 400,
  );

  // ─────────────────────────────────
  // 🏆 Reward / Result
  // ─────────────────────────────────

  static const rewardPopup = Duration(
    milliseconds: 700,
  );

  static const expFill = Duration(
    milliseconds: 900,
  );

  static const scrollReveal = Duration(
    milliseconds: 600,
  );
}