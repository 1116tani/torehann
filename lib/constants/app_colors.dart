import 'package:flutter/material.dart';

class AppColors {
  // ── 背景系 ─────────────────────
  static const background = Color(0xFF1C1610);
  static const surface = Color(0xFF2C2318);
  static const surfaceLight = Color(0xFF3D2B1F);

  // ── メインカラー ───────────────
  static const primary = Color(0xFFB8860B);
  static const secondary = Color(0xFFC8A97A);

  // ── テキスト ───────────────────
  static const textPrimary = Color(0xFFF5EDD8);
  static const textSecondary = Color(0xFFC8A97A);
  static const textMuted = Color(0xFF7A5C3A);

  // ── ボーダー ───────────────────
  static const border = Color(0xFF5C4033);
  static const divider = Color(0xFF4A3728);

  // ── 状態色 ─────────────────────
  static const success = Color(0xFF57D6C9);
  static const warning = Color(0xFFFFB347);
  static const error = Color(0xFFE57373);

  // ── ランク色 ───────────────────
  static const bronze = Color(0xFFCD7F32);
  static const silver = Color(0xFFC0C0C0);
  static const gold = Color(0xFFFFD700);

  // ── オーバーレイ ───────────────
  static const overlayDark = Color(0x99000000);
}