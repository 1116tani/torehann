import 'package:flutter/material.dart';

class AdventureSettingPage extends StatelessWidget {
  const AdventureSettingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Adventure Setting Page')));
  }
}
